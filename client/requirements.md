## Packages
recharts | Dashboard analytics charts and data visualization
date-fns | Human-readable date formatting for logs and user lists
framer-motion | Smooth animations for page transitions and layout

## Notes
Authentication is handled by Replit Auth.
All API requests must include `credentials: "include"` (handled by apiRequest/hooks).
Dashboard requires a sidebar layout.
